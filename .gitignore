.DS_Store
.phpunit.cache/

# The test harness installs into its own vendor directory so that the vendor
# directory shipped with the plugin keeps holding nothing but the autoloader
# and MailerSend's Symfony transport.
tests/vendor/
tests/composer.lock
