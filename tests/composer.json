{
    "name": "getgrav/email-mailersend-tests",
    "description": "Test harness for the Email MailerSend plugin. Kept out of the plugin's own composer.json so that the vendor directory shipped with the plugin stays free of development packages.",
    "type": "project",
    "license": "MIT",
    "require-dev": {
        "phpunit/phpunit": "^11.5",
        "symfony/http-client": "^5.4",
        "symfony/mailer": "^5.4",
        "symfony/mime": "^5.4"
    },
    "autoload-dev": {
        "psr-4": {
            "Grav\\Plugin\\EmailMailersend\\": "../classes/",
            "Grav\\Plugin\\EmailMailersend\\Tests\\": "./"
        }
    },
    "config": {
        "sort-packages": true
    }
}
